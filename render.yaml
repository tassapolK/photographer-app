services:
  - type: web
    name: photographer-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/index-lite.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /health
